{
  "name": "test",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
  "dev": "vite",
  "build": "vite build",
  "preview": "vite preview",
  "start:db": "json-server --watch db/db.json"
  },
  "devDependencies": {
    "vite": "^5.4.8"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
